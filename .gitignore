# ─── User configuration (personal, never committed) ───────────
user_config.json

# ─── Private / sensitive data ─────────────────────────────────
client_secret.json
google_access.json
GATEWAY_TOKEN.txt
pending_messages.json
# data/

# ─── Credentials & secrets ────────────────────────────────────
.git-credentials
*.token
*.key
*.pem

# ─── Logs & temporary files ───────────────────────────────────
*.log
*.bak
*.tmp
/tmp/

# ─── Node / tooling (not used by Bee-Hive OS itself) ──────────
node_modules/
